// In case of multiple sites

// import { Site } from '@constants/site';
// import { getSite } from './environments';

export default function resolveGtmContainerId (): string {
  return 'GTM-XXXXXXX'

  // In case of multiple sites

  // switch (getSite()) {
  //   default:
  //   case Site.default: {
  //     return 'GTM-XXXXXXX';
  //   }
  // }
}
